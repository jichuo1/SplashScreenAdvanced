<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="PackageVisibilityPolicy,QueryAllPackagesPermission" />

    <!-- 离线超分工厂: 批量处理可能持续数十秒, 需要前台服务保证可完成 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission
        android:name="${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        tools:node="remove" />

    <permission
        android:name="${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        tools:node="remove" />

    <application
        android:name=".SplashScreenAdvancedApp"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:description="@string/xposed_desc"
        android:theme="@style/Starting">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name=".Home"
            android:enabled="true"
            android:exported="true"
            android:label="@string/app_name"
            android:targetActivity=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!--
            离线超分缓存的只读通道

            必须 exported: 使用方 SystemUI 属于 system_app 域, SELinux 不允许它直接读取本应用
            私有目录, 只能走 Binder。不加自定义权限是因为"加签名级权限反而让 SystemUI 读不到",
            而缓存内容本身是应用图标这一公开信息。
        -->
        <provider
            android:name=".utils.sr.IconCacheProvider"
            android:authorities="com.SplashScreenAdvanced.xposedmodule.iconcache"
            android:exported="true"
            android:grantUriPermissions="false" />

        <service
            android:name=".utils.sr.IconScanService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>