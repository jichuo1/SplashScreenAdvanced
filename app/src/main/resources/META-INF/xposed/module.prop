# 101 即可加载 Hook；102 才有热重载 / autoHotReload / HookedTarget。
# 代码里所有 @SinceApi(102) 调用都要有 apiVersion 守卫，否则 XposedNewApi lint 会失败。
minApiVersion=101
targetApiVersion=102
staticScope=false
autoHotReload=true
